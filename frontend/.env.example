# Адрес backend, на который Next.js проксирует /api/* (dev-режим и сборка; в Docker — build arg)
BACKEND_INTERNAL_URL=http://localhost:8000

# Название, отображаемое в панели (единственная публичная переменная; секретов во frontend нет)
NEXT_PUBLIC_APP_NAME=Домашняя выпечка
